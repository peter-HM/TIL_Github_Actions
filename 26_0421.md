Github actions Deploy

### Step 10. Prepareing Github secrets

- DOCKERHUB_USERNAME
- DOCKERHUB_TOKEN
- EC2_HOST
- EC2_USERNAME
- EC2_SSH_KEY

to Github repo's secrets

### Step 11. Drafting a GitHub Actions workflow

Make ".gihub/workflow/deploy.yml"

```

Push to main
> checkout
> docker login
> docker build
> docker push
> EC2 SSH connect
> docker pull
> docker compose up -d
> health check 

```

### Step 12. Automated Deployment Testing

Test list

Modify the code > git push origin main > Check Actons > Check in EC2

### Step 13. Final stabilization

1. Normal log recording

- docker compose -f docker-compose.prod.yml logs app : 로그 확인이 가능 --> 3.134.216.108 미국 IP  Github actions가 배포 후 health check를 했음

2. Container status after reboot

- Restart Policy : docker-compose.prod.yml - app, db에 restart: unless-stopped 를 추가해 서버가 reboot 되어도 자동으로 재시작되게 설정하였음

- 수정 후 scp docker-compose.prod.yml mbd-page:~/mbd_page/ 를 통해 직접 EC2에 올림 --> 그후 container를 다시 올리고 sudo reboot를 통해 확인

3. Upload/DB retention
4. Availability of log verification in case of errors

- 실시간 로그 보기
  - docker compose -f docker-compose.prod.yml logs -f app

- 최근 50줄만 보기
  - docker compose -f docker-compose.prod.yml logs --tail 50 app

- 에러만 필터링
  - docker compose -f docker-compose.prod.yml logs app | grep -i error

- DB 로그 보기
  - docker compose -f docker-compose.prod.yml logs db


# TIA

- Port 추가는 요금에 영향을 미치지 않음

- 0.0.0.0/0 으로 열었기에 보안성은 취약할 수 있으나, 어디서나 연결이 가능하고 github actions의 workflow가 접근하기 위해서는 해당 주소가 필요 --> 추후에 terraform으로 actions만 허용하는 방법이 있다고 함.

- docker compose -f docker-compose.prod.yml up -d : -d 옵션의 역할 백그라운드 실행

- SSH키 github secrets에 적기 (${{ secrets.EC2_SSH_KEY }}) : cat sshkeyPath를 통해 파일을 읽어어고 그 후 ----begin 부터 end----- 까지 전부, 앞뒤 공백없이 복사해서 붙여 넣어야함.

- yaml과 yml 확장자는 완벽히 동일함. 허나 yml를 더 많이 사용하는 경향이 존재.

- git check out dev -> git commit -am "message" --> git push --> git checkout main --> git merge dev --> git push --> workflow 실행

- 2026년 6월 이후에 MarketPlace 에서 가져온 checkout ... etc 가 문제 발생 가능성이 있기에 version을 관리해야할 듯

- 멀티 플랫폼 빌드를 위해선 buildx 드라이버 설정이 요구됨. multi platform build push 이전의 step으로 다뤄줘야함.

- ls -al | grep -A 3 word : 원하는 단어를 포함한 3줄을 표기

- docker compose -f 옵션 : 어떤 compose 파일을 사용할지 지정. -f 뒤에 파일을 compose 파일로 지정, 이 옵션이 없다면 docker-compose.yml을 기본값

- logs -f app 옵션 : -f를 통해 실시간으로 적용 foward ground



