<h2> AWS </h2>

Iaas  Infra as a service

Paas Platform as a service - 환경 구축없이 서비스 배포 가능

무중단 배포를 위해 로드밸런스, 롤링 배포  Nginx를 통해 직접 구축가능하다.

정적 IP 할당이 필요하다. Network load balance

프로젝트
Spring boot 
test code

--> github에다가 push를 진행 --> trigger --> github action이 이루어진다.

github action
1. test
2. build
3. deploy

--> deploy at aws --> 하나만 있으면 무중단 배포가 불가

--> load balance (트랙픽 분산) --> EC2 connect
--> copy EC2 server --> 검사를 통과하게 되면 원본을 지우면서 무중단 배포를 사용
And load balacne expand EC2 server follow user's using or request
On demand, server increase or decrease

docker not included

--------------

IP, Packet, Pot

1. 서킷 트레이서 / 서킷 스위칭 : 선이 늘어나야 한다.
A ---> B
A ---> C
A ---> D 
가장 가까운거리로 선을 이어서 데이터를 전달한다.

2. 패킷 스위칭

발신자들의 router(hub), 수신자들의 router(hub) 를 만들고 그 hub 간의 데이터 전송
발신하는 데이터를 패킷으로 쪼개고 헤드에 정보를 입력해준다.(=Header) (재조립을 위한 info, 발신지..)
router가 패킷 포워딩을 한다. 
수신 받은 곳에서 데이터를 재조립한다.
- 선이 들어나지 않음, 비용의 감소

port : 수신하는 곳 (목적지)에 도착을 해서 어떤 프로그램에 전송할지, 결정을 하게끔 도와주는 것. 2의 16승 

----------------

EC2 서버

방확벽은 모든 포트를 차단하는 역활이다. 이를 위해서는 인바운드 규칙 수정을 통해 특정 포트를 허용해주어야 한다. ex) ssh - 22

----------------

Ppa - persion packet archive

Ubuntu lauchpad ppa - search wanted package - can add pkg to apt
cd etc/apt 로 확인 가능 resource.list.d
ubuntu version history - wiki or  lsb_release -a

-----------------

deployment

netstat

nohup - 리눅스에서 프로세스를 실행한 터미널의 세션 연결이 끊어지더라도 지속적으로 동작 할 수 있게 하는 cmd   단, 포그라운드로 실행하는 것이 아닌 백그라운드로 실행해야함

nohup은 자동으로 log를 남겨준다. 
~/project/build/libs/nohup.out
표준 출력, 에러 출력 모두 남음

* exec cmd > mylog.out & 으로 하면 로그가 남는가?

exec cmd 1>log.out 2>err.out &  --> project 내부에서 터진것은 log.out

timesetting

tiemdatectl

timedatectl list-timezones | grep -i seoul

sudo timedatctl set-timezone Asia/seoul

----------------

ps -ef | grep *.jar | grep -v grep | awk '{print $2}'
--> pgrep -f *.jar

restart-stop.sh

#!/bin/bash

echo "STOP"

PID= $(pgrep -f *.jar)

echo $PID

kill -9 $PID

-----------------

부하, 에러 --> 서버 종료
서버가 종료되었다는 로그를 남긴다. nohup 2>err.out
재시작 자동화 --> cron

